{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cypress run",
    "test:open": "cypress open",
    "test:json": "cypress run --reporter json --reporter-options output=cypress-report.json"
  },
  "devDependencies": {
    "cypress": "^13.6.3",
    "typescript": "^5.3.3"
  }
}